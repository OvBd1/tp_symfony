{% extends 'base.html.twig' %}

{% block title %}Ajouter un Commentaire - MyBlog{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-lg-6">
        <div class="card shadow">
            <div class="card-body p-5">
                <h2 class="card-title mb-4">Ajouter un commentaire à "{{ post.title }}"</h2>

                {{ form_start(form) }}
                    <div class="mb-3">
                        {{ form_label(form.content, 'Votre commentaire') }}
                        {{ form_widget(form.content, {'attr': {'class': 'form-control', 'rows': '5'}}) }}
                        <div class="text-danger small mt-1">{{ form_errors(form.content) }}</div>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <a href="{{ path('app_post_show', {id: post.id}) }}" class="btn btn-secondary">Annuler</a>
                        <button type="submit" class="btn btn-primary">Envoyer</button>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
